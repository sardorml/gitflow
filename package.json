{
  "name": "gitflow-playground",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Learn git by doing — a markdown editor, a real git terminal, and a live-animated commit graph.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "driver.js": "^1.6.0",
    "express": "^4.21.2",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
